#ifndef TABLERO_H
#define TABLERO_H
#include "lista.h"
#include <iostream>

class tablero
{
private:
    lista posiciones;
public:
    tablero();
    void generar();
    nodo* acomodar(int x, int y,ficha ficha);
    lista getPosiciones();
    void Puntuacion();
};
#endif // TABLERO_H
